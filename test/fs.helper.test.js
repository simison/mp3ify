describe('FS Helper', function () {

    describe('#isFile()', function () {
        it('should return a promise that resolves true if the supplied parameter is a file');
    });

    describe('#isDirectory()', function () {
        it('should return a promise that resolves true if the supplied parameter is a directory');
    });
});